#include <stdio.h>

/**
 * main - prints largest prime factor.
 * Return: Always 0.
 */

int main(void)
{
long int num, factor;

n = 612852475143;
for (factor = 2; factor <= num; factor++)
{
if (num % factorp == 0)
{
num /= factor;
factor--;
}
}
printf("%ld\n", factor);
return (0);
}
